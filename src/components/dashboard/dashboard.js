import React from 'react'
import authReference from '../login/auth.reference'

export default class Dashboard extends React.Component{
    constructor(props) {
        super(props)

    }

    render() {
        let fb = authReference
        debugger
        return (
            <div> This is the dashboard </div>
        )
    }
}
